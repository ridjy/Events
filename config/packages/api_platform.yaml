api_platform:
  title: "API Events"
  description: "API gestion des inscriptions aux évènements"
  version: "0.0.0"
  show_webby: true
  mapping:
    paths: ["%kernel.project_dir%/src/Entity"]
  patch_formats:
    json: ["application/merge-patch+json"]
  swagger:
    versions: [3]
    api_keys:
      apiKey:
        name: Authorization
        type: header
